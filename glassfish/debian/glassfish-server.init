#! /bin/sh

case "$1" in
	start)
		glassfish-tool start || exit 0
	;;
	restart)
		$0 stop
		$0 start
	;;
	stop)
		glassfish-tool stop || exit 0
	;;
	*)
		echo $"usage: $0 {start|stop|restart}"
		exit 3
	;;
esac
